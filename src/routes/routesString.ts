enum SITE_MAP {
   MAIN = "/",
   CALENDAR = "/calendar",
   ANALYTICS = "/analytics",
   ADS = "/ads",
   CAMPAIGNS = "/campaigns",
   SETTINGS = "/settings",

   LOGIN = "/login",
   REGISTER = "/register",

   NOTFOUND = "*",
   FORBIDDEN = "/forbidden",
}

export default SITE_MAP;
